import { Text, StyleSheet, View, Button } from 'react-native'

export default function BottomSheet() 
{
    return(
        <View>
            <Text>Proximamente by: Valeria o Isabella</Text>
        </View>
    )
}

const styles = StyleSheet.create({})