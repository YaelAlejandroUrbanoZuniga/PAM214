import { Text, StyleSheet, View, Button } from 'react-native'

export default function ScrollView() 
{
    return(
        <View>
            <Text>Proximamente by: Isaac Alvarez</Text>
        </View>
    )
}

const styles = StyleSheet.create({})